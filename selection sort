def selectionsort(arr):
   for i in range(len(arr)):
       min = i
       for j in range(i+1,len(arr)):
           if arr[min] > arr[j]:
       arr[i],arr[min] = arr[min],arr[i]
       
arr = [3,4,5,7,1]
selectionsort(arr)
for i in range(len(arr)):
   print(arr[i])
   
